info:
  name: Participants - DELETE remove
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{baseUrl}}/trips/:tripId/participants/:participantId'
  params:
    - name: tripId
      value: '{{tripId}}'
      type: path
    - name: participantId
      value: '{{participantId}}'
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
