info:
  name: Trips - DELETE /trips/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{baseUrl}}/trips/:id'
  params:
    - name: id
      value: '{{tripId}}'
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
