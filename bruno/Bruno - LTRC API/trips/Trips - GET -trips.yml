info:
  name: Trips - GET /trips
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/trips?page=1&size=10'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
