info:
  name: Trips - GET /trips/:id
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/trips/:id'
  params:
    - name: id
      value: '{{tripId}}'
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
